'use client';
import { useState } from 'react';
import './admin.css';
export default function Admin(){const [admins,setAdmins]=useState(['aaravg78201333@gmail.com']);const [email,setEmail]=useState(''); return <main className="admin"><div className="adminbox"><div className="brand"><span className="cube">◆</span> ODARIS <b>MC</b></div><h1>Admin Panel</h1><p className="muted">Owner & administration center</p><div className="notice">🔐 Production authentication must be connected before this panel is exposed publicly. This starter does not store passwords or secrets.</div><section><h2>Administrators</h2>{admins.map((a,i)=><div className="adminrow" key={a}><span>{a}</span>{i>0&&<button onClick={()=>setAdmins(admins.filter(x=>x!==a))}>Remove</button>}{i===0&&<small>OWNER</small>}</div>)}<div className="add"><input value={email} onChange={e=>setEmail(e.target.value)} placeholder="admin@example.com"/><button onClick={()=>{if(email&&!admins.includes(email)){setAdmins([...admins,email]);setEmail('')}}}>Add Admin</button></div></section><section><h2>Store management</h2><div className="stats"><div><b>6</b><small>Products</small></div><div><b>₹0</b><small>Sales</small></div><div><b>0</b><small>Orders</small></div></div></section><a className="back" href="/">← Back to store</a></div></main>}
